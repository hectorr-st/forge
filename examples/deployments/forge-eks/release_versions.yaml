---
api_version: v1
product: Forge
version: v1.0.0
metadata:
  use_local_repos: true
spec:
  # Versions of our IaC/Terraform modules.
  iac:
    modules:
      eks:
        local_path: ../forge
        repo: git@github.com:cisco-open/forge.git
        module_path: modules/infra/eks
        ref: main
      eks_secrets:
        local_path: ../forge
        repo: git@github.com:cisco-open/forge.git
        module_path: modules/infra/eks_secrets
        ref: main
